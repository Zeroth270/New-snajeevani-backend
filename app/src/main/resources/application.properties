# This file is intentionally blank.
# All configuration has been moved to application.yml (multi-profile YAML).
# Spring Boot loads YAML with higher precedence when both exist, but keeping
# this file empty prevents any accidental property leakage.
